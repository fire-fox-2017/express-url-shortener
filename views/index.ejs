<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
      <div class="">
        <form class="" action="/add" method="post">
          <button type="submit" name="button">Add Url</button>
        </form>
      </div>
      <br>
      <div class="">
        <table>
          <thead>
            <th>No</th>
            <th>Url</th>
            <th>Counter</th>
          </thead>
          <tbody>
            <%let no = 1%>
            <%url.forEach((data)=>{%>
            <tr>
              <th><%= no %></th>
              <th><a href="/<%=data.id%>" name="short"><%= data.short_url %></a></th>
              <th><%= data.counter %></th>
            </tr>
            <%no++ })%>
          </tbody>
        </table>
      </div>
  </body>
</html>
